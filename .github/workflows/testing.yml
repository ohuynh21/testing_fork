name: Testing Workflow
on:
  workflow_run:
    workflows:
      - "Deploy GitHub Pages"
    types:
      - completed

jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run Tests
        run: npm install && npm test
